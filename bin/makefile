# Makefile for compiling PeerProcess.java

# Set the Java compiler
JAVAC = javac

# Source file
SOURCE_FILE = PeerProcess.java

# Compilation target
TARGET = PeerProcess

all: $(TARGET)

$(TARGET): $(SOURCE_FILE)
	$(JAVAC) $(SOURCE_FILE)

clean:
	rm -f $(TARGET).class